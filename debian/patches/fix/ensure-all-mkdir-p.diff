Index: vrpn-07.22/quat/makefile
===================================================================
--- vrpn-07.22.orig/quat/makefile
+++ vrpn-07.22/quat/makefile
@@ -173,7 +173,7 @@ LINT_FLAGS = $(INCLUDE_FLAGS)
 lib:	$(HW_OS)$(OBJECT_DIR_SUFFIX) libquat.a
 
 $(HW_OS)$(OBJECT_DIR_SUFFIX):
-	-mkdir $(HW_OS)$(OBJECT_DIR_SUFFIX)
+	-mkdir -p $(HW_OS)$(OBJECT_DIR_SUFFIX)
 
 #############################################################################
 #
Index: vrpn-07.22/vrpn/client_src/Makefile
===================================================================
--- vrpn-07.22.orig/vrpn/client_src/Makefile
+++ vrpn-07.22/vrpn/client_src/Makefile
@@ -146,15 +146,15 @@ LIB_DIR := ../$(OBJ_DIR)
 override LFLAGS := -L$(LIB_DIR) -L../../quat/$(HW_OS)$(OBJECT_DIR_SUFFIX) $(DEBUG_FLAGS) $(LDFLAGS)
 
 $(OBJ_DIR)/%.o: %.c
-	@[ -d $(OBJ_DIR) ] || mkdir $(OBJ_DIR)
+	@[ -d $(OBJ_DIR) ] || mkdir -p $(OBJ_DIR)
 	$(CC) $(CFLAGS) -o $@ -c $<
 
 $(OBJ_DIR)/%.o: %.C
-	@[ -d $(OBJ_DIR) ] || mkdir $(OBJ_DIR)
+	@[ -d $(OBJ_DIR) ] || mkdir -p $(OBJ_DIR)
 	$(CC) $(CXXFLAGS) -o $@ -c $<
 
 $(OBJ_DIR)/%.o: %.cpp
-	@[ -d $(OBJ_DIR) ] || mkdir $(OBJ_DIR)
+	@[ -d $(OBJ_DIR) ] || mkdir -p $(OBJ_DIR)
 	$(CC) $(CXXFLAGS) -o $@ -c $<
 
 INSTALL_APPS := vrpn_print_devices forcedevice_test_client vrpn_ping \
@@ -324,7 +324,7 @@ $(OBJ_DIR)/logfiletypes: $(OBJ_DIR)/logf
 	$(CC) $(LFLAGS) -o $(OBJ_DIR)/logfiletypes $(OBJ_DIR)/logfiletypes.o
 
 install: all
-	-mkdir $(BIN_DIR)
+	-mkdir -p $(BIN_DIR)
 	( cd $(BIN_DIR) ; rm -f $(INSTALL_APPS) )
 	( cd $(OBJ_DIR) ; cp $(INSTALL_APPS) $(BIN_DIR) )
 	( cd $(BIN_DIR) ; strip $(INSTALL_APPS) )
Index: vrpn-07.22/vrpn/server_src/Makefile
===================================================================
--- vrpn-07.22.orig/vrpn/server_src/Makefile
+++ vrpn-07.22/vrpn/server_src/Makefile
@@ -200,15 +200,15 @@ endif
 	$(CC) $(CXXFLAGS) -c $*.cpp
 
 $(OBJ_DIR)/%.o:	%.c
-	@[ -d $(OBJ_DIR) ] || mkdir $(OBJ_DIR)
+	@[ -d $(OBJ_DIR) ] || mkdir -p $(OBJ_DIR)
 	$(CC) $(CFLAGS) -o $@ -c $<
 
 $(OBJ_DIR)/%.o:	%.C
-	@[ -d $(OBJ_DIR) ] || mkdir $(OBJ_DIR)
+	@[ -d $(OBJ_DIR) ] || mkdir -p $(OBJ_DIR)
 	$(CC) $(CXXFLAGS) -o $@ -c $<
 
 $(OBJ_DIR)/%.o:	%.cpp
-	@[ -d $(OBJ_DIR) ] || mkdir $(OBJ_DIR)
+	@[ -d $(OBJ_DIR) ] || mkdir -p $(OBJ_DIR)
 	$(CC) $(CXXFLAGS) -o $@ -c $<
 
 INSTALL_APPS := vrpn_server test_vrpn
@@ -378,7 +378,7 @@ $(OBJ_DIR)/testSharedObject:	$(OBJ_DIR)/
 		$(VRPN_LIBS) $(SYSLIBS) -lm
 
 install: all
-	-mkdir $(BIN_DIR)
+	-mkdir -p $(BIN_DIR)
 	( cd $(BIN_DIR) ; rm -f $(INSTALL_APPS) )
 	( cd $(OBJ_DIR) ; cp $(INSTALL_APPS) $(BIN_DIR) )
 	( cd $(BIN_DIR) ; strip $(INSTALL_APPS) )
Index: vrpn-07.22/vrpn/java_vrpn/makefile
===================================================================
--- vrpn-07.22.orig/vrpn/java_vrpn/makefile
+++ vrpn-07.22/vrpn/java_vrpn/makefile
@@ -282,18 +282,18 @@ CFLAGS		 := $(INCLUDE_FLAGS) -g
 # Build objects from .cpp files
 #$(OBJECT_DIR)/%.o: %.cpp $(LIB_INCLUDES) $(MAKEFILE)
 $(OBJECT_DIR)/%.o: %.cpp $(LIB_INCLUDES) 
-	@[ -d $(OBJECT_DIR) ] || mkdir $(OBJECT_DIR)
+	@[ -d $(OBJECT_DIR) ] || mkdir -p $(OBJECT_DIR)
 	$(CC) $(CFLAGS) -o $@ -c $<
 
 # Build objects from .C files
 #$(OBJECT_DIR)/%.o: %.C $(LIB_INCLUDES) $(MAKEFILE)
 #$(OBJECT_DIR)/%.o: %.C $(LIB_INCLUDES) 
-#	@[ -d $(OBJECT_DIR) ] || mkdir $(OBJECT_DIR)
+#	@[ -d $(OBJECT_DIR) ] || mkdir -p $(OBJECT_DIR)
 #	$(CC) $(CFLAGS) -o $@ -c $< $(LIBS) -lvrpn -exceptions
 
 # Build objects from .C files
 #$(SOBJECT_DIR)/%.o: %.C $(LIB_INCLUDES) $(MAKEFILE)
-#	@[ -d $(SOBJECT_DIR) ] || mkdir $(SOBJECT_DIR)
+#	@[ -d $(SOBJECT_DIR) ] || mkdir -p $(SOBJECT_DIR)
 #	$(CC) $(CFLAGS) -o $@ -c $<
 
 #
@@ -311,10 +311,10 @@ client: $(OBJECT_DIR)/libjava_vrpn.a
 
 
 $(OBJECT_DIR):
-	-mkdir $(OBJECT_DIR)
+	-mkdir -p $(OBJECT_DIR)
 
 $(SOBJECT_DIR):
-	-mkdir $(SOBJECT_DIR)
+	-mkdir -p $(SOBJECT_DIR)
 
 #############################################################################
 #
@@ -457,7 +457,7 @@ $(OBJECT_DIR)/.depend:
 	@echo -- or add/remove includes from a .h or .cpp file, then you should
 	@echo -- remake the dependency file by typing \"$(MAKE) depend\"
 	@echo ----------------------------------------------------------------
-	-mkdir $(OBJECT_DIR)
+	-mkdir -p $(OBJECT_DIR)
 ifeq ($(HW_OS),hp700_hpux10)
 	@echo -- $(HW_OS): Using g++ since HP CC does not understand -M
 	@echo -- if this causes an error, then delete .depend and type
Index: vrpn-07.22/vrpn/Makefile
===================================================================
--- vrpn-07.22.orig/vrpn/Makefile
+++ vrpn-07.22/vrpn/Makefile
@@ -384,22 +384,22 @@ override CXXFLAGS     := $(INCLUDE_FLAGS
 
 # Build objects from .c files
 $(OBJECT_DIR)/%.o: %.c $(LIB_INCLUDES) $(MAKEFILE)
-	@[ -d $(OBJECT_DIR) ] || mkdir $(OBJECT_DIR)
+	@[ -d $(OBJECT_DIR) ] || mkdir -p $(OBJECT_DIR)
 	$(CC) $(CFLAGS) -DVRPN_CLIENT_ONLY -o $@ -c $<
 
 # Build objects from .C files
 $(OBJECT_DIR)/%.o: %.C $(LIB_INCLUDES) $(MAKEFILE)
-	@[ -d $(OBJECT_DIR) ] || mkdir $(OBJECT_DIR)
+	@[ -d $(OBJECT_DIR) ] || mkdir -p $(OBJECT_DIR)
 	$(CC) $(CXXFLAGS) -DVRPN_CLIENT_ONLY -o $@ -c $<
 
 # Build objects from .C files
 $(SOBJECT_DIR)/%.o: %.C $(SLIB_INCLUDES) $(MAKEFILE)
-	@[ -d $(SOBJECT_DIR) ] || mkdir $(SOBJECT_DIR)
+	@[ -d $(SOBJECT_DIR) ] || mkdir -p $(SOBJECT_DIR)
 	$(CC) $(CXXFLAGS) -o $@ -c $<
 
 # Build objects from .C files
 $(AOBJECT_DIR)/%.o: %.C $(ALIB_INCLUDES) $(MAKEFILE)
-	@[ -d $(AOBJECT_DIR) ] || mkdir $(AOBJECT_DIR)
+	@[ -d $(AOBJECT_DIR) ] || mkdir -p $(AOBJECT_DIR)
 	$(CC) $(CXXFLAGS) -o $@ -c $<
 
 #
@@ -446,13 +446,13 @@ atmellib: $(AOBJECT_DIR)
 	$(MAKE) $(OBJECT_DIR)/libvrpnatmel.a
 
 $(OBJECT_DIR):
-	-mkdir $(OBJECT_DIR)
+	-mkdir -p $(OBJECT_DIR)
 
 $(SOBJECT_DIR):
-	-mkdir $(SOBJECT_DIR)
+	-mkdir -p $(SOBJECT_DIR)
 
 $(AOBJECT_DIR):
-	-mkdir $(AOBJECT_DIR)
+	-mkdir -p $(AOBJECT_DIR)
 
 #############################################################################
 #
